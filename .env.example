# Bedrock Agent Configuration (optional - will use Claude 3.5 Sonnet directly if not set)
BEDROCK_AGENT_ID=your-agent-id-here
BEDROCK_AGENT_ALIAS_ID=TSTALIASID
AWS_REGION=us-east-1

# Cognito Configuration (required)
COGNITO_USER_POOL_ID=your-user-pool-id
COGNITO_CLIENT_ID=your-client-id

# DynamoDB Configuration
SESSIONS_TABLE=tenant-sessions
USAGE_TABLE=tenant-usage

# Application Configuration
APP_HOST=0.0.0.0
APP_PORT=8000

# Security
JWT_SECRET=your-jwt-secret-key