# Plan: Triage Fixes -- 2026-03-28

## Task Description
Address 2 infrastructure gaps identified by /triage diagnostic scan. No application-level bugs were found -- all issues are operational/observability improvements.

## Objective
Close observability gaps (Langfuse rate limiting, CloudWatch Logs Insights permissions) and investigate low dev environment usage to ensure the environment is ready for active development and testing.

## Problem Statement
The dev environment is healthy but has two observability blind spots: (1) the CI deploy role cannot run CloudWatch Logs Insights queries, limiting automated triage capability, and (2) Langfuse API rate limits block trace retrieval during CI triage runs. Additionally, zero user traffic in 24h suggests the dev environment may be underutilized.

## Relevant Files
- `infrastructure/cdk-eagle/lib/ci-cd-stack.ts` -- Deploy role IAM policy (needs logs:StartQuery, logs:GetQueryResults, logs:StopQuery)
- `server/.env` -- Langfuse credentials (check if shared across CI jobs causing rate limits)
- `.github/workflows/` -- CI workflow that runs triage (may need rate limiting awareness)

## Implementation Phases

### Phase 1: P2 Fixes (Medium)

#### 1. Add CloudWatch Logs Insights permissions to deploy role

- **File**: `infrastructure/cdk-eagle/lib/ci-cd-stack.ts` (or whichever stack defines eagle-deploy-role-dev)
- **Problem**: The eagle-deploy-role-dev IAM role lacks logs:StartQuery, logs:GetQueryResults, logs:StopQuery, and logs:DescribeLogGroups permissions. This blocks CloudWatch Logs Insights queries from CI triage.
- **Fix**: Add an IAM policy statement granting these 4 actions scoped to /eagle/* log groups:
  - logs:StartQuery
  - logs:GetQueryResults
  - logs:StopQuery
  - logs:DescribeLogGroups
  Resource: arn:aws:logs:us-east-1:*:log-group:/eagle/*
- **Validation**:
  - npx cdk synth --quiet (CDK compiles)
  - Redeploy CI/CD stack
  - Run aws logs start-query with the deploy role to confirm access

### Phase 2: P3 Improvements (Low)

#### 2. Add Langfuse rate-limit resilience to triage workflow

- **File**: `.claude/commands/triage.md`
- **Problem**: Langfuse returns HTTP 429 when queried during CI runs, leaving a trace data gap in the report.
- **Fix**: The triage command already handles Langfuse unavailability gracefully (continues with 2/3 sources). No code change needed. Consider adding a retry with exponential backoff (1s, 2s, 4s) in the Langfuse fetch script for transient rate limits.
- **Validation**: Run /triage light and confirm Langfuse section either returns data or reports the gap cleanly.

#### 3. Investigate dev environment utilization

- **File**: N/A (operational)
- **Problem**: Zero user traffic in 24h may indicate the dev environment is idle or users have moved to QA.
- **Fix**: Verify with the team whether dev is expected to be active. If idle by design, consider scaling down to save costs (desiredCount=0 when not in use).
- **Validation**: Check with team; review ALB access logs for the prior 7 days.

## Step by Step Tasks

### 1. Grant CloudWatch Logs Insights permissions to deploy role
- **File**: infrastructure/cdk-eagle/lib/ci-cd-stack.ts
- **Problem**: logs:StartQuery denied for eagle-deploy-role-dev
- **Fix**: Add IAM policy statement with logs:StartQuery, logs:GetQueryResults, logs:StopQuery, logs:DescribeLogGroups on /eagle/* log groups
- **Validation**: npx cdk synth --quiet passes; post-deploy aws logs start-query succeeds

### 2. (Optional) Add Langfuse retry logic
- **File**: .claude/commands/triage.md (Langfuse fetch script)
- **Problem**: Single-attempt Langfuse API call fails on rate limit
- **Fix**: Add 3-attempt retry with 1s/2s/4s backoff before declaring Langfuse unavailable
- **Validation**: /triage light completes without error

### 3. Validate all fixes
- ruff check app/ -- Python lint
- npx tsc --noEmit -- TypeScript check
- npx cdk synth --quiet -- CDK compile
- Re-run /triage light to confirm CloudWatch Logs Insights queries succeed

## Acceptance Criteria
- CloudWatch Logs Insights queries succeed from the CI deploy role
- Triage report captures all 3 data sources (or gracefully degrades with documented gaps)
- No new errors introduced (re-triage shows same or improved state)
- CDK synth passes after IAM policy changes

## Validation Commands
- ruff check app/ -- Python lint
- npx tsc --noEmit -- TypeScript check
- npx cdk synth --quiet -- CDK compile
- /triage light -- Re-triage to confirm improvement

## Notes
- Generated by /triage on 2026-03-28
- Triage report: docs/development/20260328-091100-report-triage-dev-v4.md
- Cross-referenced 0 feedback items, 0 CloudWatch errors, 0 Langfuse traces (rate-limited)
- Environment is stable -- this plan focuses on observability improvements, not bug fixes
