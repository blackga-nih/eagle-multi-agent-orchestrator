# Plan: Triage Fixes — 2026-04-01

## Task Description
Fix 2 issues identified by /triage diagnostic scan across DynamoDB feedback, CloudWatch logs, and Langfuse traces (dev environment).

## Objective
Resolve all P1 issues to restore full observability and knowledge search AI ranking in the dev environment.

## Problem Statement
The OpenTelemetry OTLP span exporter is continuously failing with HTTP 401 Unauthorized (91 occurrences in 24h), causing all trace data to be silently dropped. Separately, the knowledge search AI ranking feature is broken because the ECS task role only permits Haiku 4.5 but the code defaults to the old Claude 3 Haiku model ID.

## Relevant Files
- `server/app/tools/knowledge_tools.py` — Update default model ID from claude-3-haiku to claude-haiku-4-5
- `infrastructure/cdk-eagle/lib/core-stack.ts` — Verify Bedrock IAM policy (already correct for Haiku 4.5)
- `server/app/strands_agentic_service.py` — OTel OTLP exporter configuration (lines 99-121)
- `server/.env` — Langfuse credentials used for OTLP auth

## Implementation Phases

### Phase 1: P1 Fixes (Critical)

#### Fix 1: Update knowledge_search model ID to Haiku 4.5
The SEARCH_MODEL_ID in knowledge_tools.py defaults to anthropic.claude-3-haiku-20240307-v1:0 which is not permitted by the IAM policy. Update to match the allowed model.

#### Fix 2: Diagnose and fix OTel OTLP 401 Unauthorized
The OTLP exporter in strands_agentic_service.py uses Langfuse public/secret keys for Basic auth to the Langfuse OTEL endpoint. The 401 suggests credentials are invalid, expired, or the endpoint format has changed.

### Phase 2: P2 Improvements (Medium)

#### Improvement 1: Add startup validation for OTLP credentials
Add a lightweight health check at startup that validates the OTLP endpoint responds with 200 before enabling the exporter, logging a clear warning if auth fails.

## Step by Step Tasks

### 1. Fix knowledge_search model ID (P1)
- **File**: server/app/tools/knowledge_tools.py (line ~285)
- **Problem**: SEARCH_MODEL_ID defaults to anthropic.claude-3-haiku-20240307-v1:0 — not in IAM allow-list
- **Fix**: Change default to anthropic.claude-haiku-4-5-20251001-v1:0 to match the IAM policy in core-stack.ts line 157. Alternatively, use the cross-region inference profile us.anthropic.claude-haiku-4-5-20251001-v1:0 for consistency with how the SDK invokes models.
- **Validation**:
  - ruff check server/app/tools/knowledge_tools.py
  - python -m pytest tests/ -v -k knowledge (if knowledge tests exist)
  - Deploy to dev and verify knowledge_search no longer logs AccessDeniedException

### 2. Diagnose OTel OTLP 401 Unauthorized (P1)
- **File**: server/app/strands_agentic_service.py (lines 99-121)
- **Problem**: OTLP exporter sends spans to https://us.cloud.langfuse.com/api/public/otel/v1/traces with Basic auth using LANGFUSE_PUBLIC_KEY:LANGFUSE_SECRET_KEY. Getting 401.
- **Diagnosis steps**:
  1. Verify the Langfuse API keys in the ECS task environment match the keys in server/.env — they may differ from what is deployed
  2. Test the OTLP endpoint manually with curl
  3. Check if Langfuse project has been recreated or keys rotated since last deploy
  4. Check if the OTLP endpoint URL format has changed (Langfuse occasionally updates their API)
- **Fix**: Update the Langfuse credentials in the ECS task definition (via CDK environment variables or SSM Parameter Store) to match valid, current Langfuse project keys. If the endpoint format changed, update the LANGFUSE_OTEL_ENDPOINT env var.
- **Validation**:
  - After deploy, check CloudWatch for absence of OTLP 401 errors
  - Verify traces appear in Langfuse dashboard

### 3. Validate All Fixes
- ruff check app/
- npx tsc --noEmit
- python -m pytest tests/ -v
- Re-run /triage light to confirm issues resolved

## Acceptance Criteria
- Knowledge search AI ranking uses Haiku 4.5 model ID matching IAM policy
- No more AccessDeniedException errors in CloudWatch for bedrock:InvokeModel
- OTel OTLP exporter successfully authenticates (no more 401 errors)
- Langfuse traces are being ingested for backend operations
- No new errors introduced (re-triage shows improvement)
- Validation commands pass

## Validation Commands
- ruff check app/ — Python lint
- npx tsc --noEmit — TypeScript check
- python -m pytest tests/ -v — Unit tests
- /triage light — Re-triage to confirm improvement

## Notes
- Generated by /triage on 2026-04-01
- Triage report: docs/development/20260401-090000-report-triage-dev-v5.md
- Cross-referenced 0 feedback items, 93 CloudWatch errors, 0 Langfuse traces (rate-limited)
- Langfuse trace data was unavailable due to API rate limiting — re-triage recommended after fixes to validate with full data
