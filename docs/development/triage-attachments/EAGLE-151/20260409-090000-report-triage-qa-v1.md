# EAGLE Triage Report

**Date**: 2026-04-09
**Environment**: qa
**Window**: 24h (2026-04-08 00:00 UTC to 2026-04-09 23:59 UTC)
**Tenant**: default-qa
**Mode**: Full
**Sources**: DynamoDB Feedback (UNAVAILABLE), CloudWatch Logs (qa), Langfuse Traces (UNAVAILABLE)

## Executive Summary

The QA environment is experiencing **persistent Bedrock model unavailability** for `us.anthropic.claude-sonnet-4-6`. The circuit breaker has been tripping continuously throughout April 9 (06:41-08:57 UTC), with failure counts escalating from 2 to 11, indicating the primary model is unreachable. One actual user-facing conversation failure was recorded on April 8 (session `495968f5`). Additionally, the **OTel/Langfuse telemetry pipeline is broken** — span exports are failing with 401 Unauthorized (~50 errors), and the Langfuse QA API keys are not configured in the environment, creating an observability blind spot.

**Data collection gaps**: DynamoDB feedback and Langfuse trace queries were unavailable due to a CI hook configuration issue (Windows path in `.claude/settings.json` blocking Bash execution). Langfuse QA-specific keys (`LANGFUSE_QA_PUBLIC_KEY`/`LANGFUSE_QA_SECRET_KEY`) are empty in `server/.env`.

## Source Data

### DynamoDB Feedback

**Status**: UNAVAILABLE — CI hook misconfiguration blocked boto3 query execution.

**Root cause**: `.claude/settings.json` contains a PreToolUse hook referencing a Windows-only path (`C:/Users/blackga/Desktop/eagle/sm_eagle/.claude/hooks/pre_tool_use.py`), which fails on Linux CI runners.

### CloudWatch Errors

**Log groups queried**: `/eagle/ecs/backend-qa`, `/eagle/ecs/frontend-qa`, `/eagle/app`

| Log Group | Records Scanned | Records Matched | Errors | Warnings |
|-----------|----------------|-----------------|--------|----------|
| `/eagle/ecs/backend-qa` | 25,695 | 143 | 56 | 85 |
| `/eagle/ecs/frontend-qa` | 0 | 0 | 0 | 0 |
| `/eagle/app` | 0 | 0 | 0 | 0 |

#### Backend QA Error Breakdown

| # | Error Pattern | Category | Count | Severity | Time Range |
|---|---------------|----------|-------|----------|------------|
| 1 | `keepalive_ping: us.anthropic.claude-sonnet-4-6 FAILED: ServiceUnavailableException` | Bedrock Availability | 33 | Warning | Apr 9 06:41-08:57 |
| 2 | `circuit_breaker: us.anthropic.claude-sonnet-4-6 -> OPEN (failures=2-11)` | Bedrock Availability | 22 | Warning | Apr 9 06:41-08:57 |
| 3 | `keepalive_ping: us.anthropic.claude-sonnet-4-6 failed — circuit breaker notified` | Bedrock Availability | 30 | Warning | Apr 9 06:41-08:57 |
| 4 | `Failed to export span batch code: 401, reason: Unauthorized` (OTel OTLP) | Telemetry Auth | ~50 | ACTIONABLE | Apr 8 14:33-14:38 |
| 5 | `Langfuse list_traces failed: 400 Bad Request` | Langfuse API | 2 | ACTIONABLE | Apr 8 06:28-06:50 |
| 6 | `cycle failed: ServiceUnavailableException: Too many connections` (ConverseStream) | Bedrock Availability | 1 | ACTIONABLE | Apr 8 06:27 |
| 7 | `ValueError: generator already executing` (OTel use_span) | OTel Async Context | 3 | Noise | Apr 8 06:46 |

#### Detailed Error Evidence

**Error 1-3: Bedrock Model Unavailability (Cluster)**

The `us.anthropic.claude-sonnet-4-6` model (via Bedrock `Converse` API) has been persistently failing in the QA environment. The circuit breaker (configured at threshold=2 in `server/app/strands_agentic_service.py:466`) opens immediately and stays open, with failure count escalating from 2 to 11 across the window. This affects all keepalive probes and at least one real user conversation.

Representative log:
```json
{"level": "WARNING", "logger": "eagle.strands_agent",
 "msg": "keepalive_ping: us.anthropic.claude-sonnet-4-6 FAILED: An error occurred (ServiceUnavailableException) when calling the Converse operation (reached max retries: 1): Bedrock is unable to process your request."}
```

**Error 4: OTel OTLP Span Export 401 Unauthorized**

The OpenTelemetry OTLP trace exporter is failing with 401 Unauthorized, meaning Langfuse credentials used for span export are invalid or expired. This creates an observability gap — no traces are being persisted.

```json
{"level": "ERROR", "logger": "opentelemetry.exporter.otlp.proto.http.trace_exporter",
 "msg": "Failed to export span batch code: 401, reason: Unauthorized"}
```

Exporter configured at: `server/app/strands_agentic_service.py:86-174` (`_ensure_langfuse_exporter()`).

**Error 5: Langfuse list_traces 400 Bad Request**

The Langfuse client's `list_traces()` call is receiving 400 responses when querying by `sessionId`. This affects trace lookup for active sessions.

Session affected: `495968f5-6ae8-46ed-b99b-d7120e09c171`
User: `64d8a488-4081-707e-0ff4-2bab1ffad3e1`

```json
{"level": "ERROR", "logger": "eagle.telemetry.langfuse_client",
 "msg": "Langfuse list_traces failed: Client error '400 Bad Request' for url '...?sessionId=495968f5-6ae8-46ed-b99b-d7120e09c171'"}
```

Client code: `server/app/telemetry/langfuse_client.py:37-96`.

**Error 6: Strands Cycle Failure (User-Impacting)**

A real conversation cycle failed with `ServiceUnavailableException: Too many connections` on `ConverseStream`. This is the user-visible manifestation of the Bedrock model unavailability.

Session affected: `495968f5-6ae8-46ed-b99b-d7120e09c171` (same session as Langfuse 400 errors)

### Langfuse Trace Errors

**Status**: UNAVAILABLE — `LANGFUSE_QA_PUBLIC_KEY` and `LANGFUSE_QA_SECRET_KEY` are empty in `server/.env`. Dev keys exist as fallback but the QA-specific project is not configured.

## Cross-Reference Analysis

### Session Correlation Map

| Session ID | Sources | Errors |
|-----------|---------|--------|
| `495968f5-6ae8-46ed-b99b-d7120e09c171` | CW (3 errors) | Langfuse 400 Bad Request (x2), Strands cycle failure (ServiceUnavailableException) |

This is the only session with identifiable errors in the 24h window. It appears in multiple CloudWatch error types, confirming a user-impacting incident on April 8 around 06:27-06:50 UTC.

### Error Pattern Clusters

| Cluster | CloudWatch Signal | Count | Assessment |
|---------|------------------|-------|------------|
| **Bedrock Availability** | ServiceUnavailableException on keepalive + ConverseStream | 86 | Model `us.anthropic.claude-sonnet-4-6` unreachable; circuit breaker opens continuously |
| **Telemetry Auth** | OTel OTLP 401 Unauthorized | ~50 | Langfuse credentials invalid for span export |
| **Langfuse API** | list_traces 400 Bad Request | 2 | API parameter issue or credential mismatch |
| **OTel Async** | ValueError: generator already executing | 3 | Known Strands SDK + OTel instrumentation race |

### Trend Analysis

- **Bedrock errors are escalating**: Circuit breaker failure count increased from 2 to 11 over ~2 hours on April 9, indicating the model is not recovering between probe intervals. This suggests a sustained Bedrock service issue, not transient throttling.
- **OTel 401 errors are clustered**: All 50 errors occurred in a 5-minute burst (14:33-14:38 on Apr 8), suggesting either a credential rotation event or a brief connectivity issue that triggered a batch retry storm.
- **Single affected user**: Only one user session (`495968f5`) experienced visible failures, suggesting low QA traffic volume.

## Prioritized Issue List

| # | Issue | Composite Score | Priority | Sources | Sessions |
|---|-------|----------------|----------|---------|----------|
| 1 | Bedrock `us.anthropic.claude-sonnet-4-6` persistently unavailable (ServiceUnavailableException) | 6 | **P0** | CW (86 events) | 1 confirmed |
| 2 | OTel OTLP span export failing with 401 Unauthorized (no traces persisted) | 4 | **P1** | CW (50 events) | N/A |
| 3 | Langfuse QA keys not configured (`LANGFUSE_QA_*` empty in .env) | 4 | **P1** | Config | N/A |
| 4 | Langfuse `list_traces` returning 400 Bad Request for session lookups | 3 | **P2** | CW (2 events) | 1 |
| 5 | CI hook path hardcoded to Windows (`settings.json`) blocks CI triage | 3 | **P2** | CI config | N/A |
| 6 | OTel `use_span` ValueError (generator already executing) | 1 | **P3** | CW (3 events) | 0 |

**Scoring rationale:**
- P0 Issue 1: User-facing=2 (blocks chat), Frequency=2 (86 events), Cross-source=1 (keepalive+cycle), Severity=1 (ACTIONABLE) = 6
- P1 Issue 2: User-facing=0, Frequency=2 (50 events), Cross-source=1 (related to Issue 3), Severity=1 = 4
- P1 Issue 3: User-facing=1 (breaks observability), Frequency=1 (persistent config gap), Cross-source=1 (root cause of Issue 2), Severity=1 = 4
- P2 Issue 4: User-facing=1, Frequency=1, Cross-source=0, Severity=1 = 3
- P2 Issue 5: User-facing=0, Frequency=1, Cross-source=1 (blocks future triage), Severity=1 = 3
- P3 Issue 6: User-facing=0, Frequency=0, Cross-source=0, Severity=0 = 0 (Noise)

## Noise Report

| Item | Count | Justification |
|------|-------|---------------|
| OTel `use_span` ValueError (generator already executing) | 3 | Known Strands SDK + OTel instrumentation async context collision. Not user-impacting. Tracked upstream. |
| Frontend QA empty log group | 0 | No frontend errors — healthy or not logging (frontend-qa log group scanned 0 records) |
| `/eagle/app` empty log group | 0 | Shared app log group had 0 records in window — may not be actively used in QA |
