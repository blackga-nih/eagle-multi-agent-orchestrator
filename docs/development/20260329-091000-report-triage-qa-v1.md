# EAGLE Triage Report

**Date**: 2026-03-29
**Environment**: qa
**Window**: 24h
**Tenant**: default-qa
**Mode**: Full
**Sources**: DynamoDB Feedback, CloudWatch Logs (qa), Langfuse Traces (qa)

## Executive Summary

The QA environment is largely quiescent. The backend-qa service is healthy (returning 200 on `/api/health`), but there is **zero user activity** — no feedback, no Langfuse traces, and no errors across all three log groups in the last 24 hours. The frontend-qa container has been idle since 2026-03-27 (~2 days), and the shared `/eagle/app` log group has had no activity since 2026-03-18 (~11 days). This suggests the QA environment is deployed but not actively used by testers or end users.

**Key finding**: No bugs or errors detected — the primary concern is the **lack of QA testing activity**, which means potential regressions are going undetected.

## Source Data

### DynamoDB Feedback

| Metric | Value |
|--------|-------|
| General feedback items | 0 |
| Message feedback items | 0 |
| Bug reports | 0 |
| Thumbs down | 0 |
| Thumbs up | 0 |

No feedback has been submitted for the `default-qa` tenant. This confirms zero user interaction with the QA environment's feedback system.

### CloudWatch Errors

#### `/eagle/ecs/backend-qa`

| Metric | Value |
|--------|-------|
| Error events (24h) | 0 |
| Total activity | Active — health check logs present |
| Sample log | `GET /api/health HTTP/1.1 200 OK` (every ~5s) |

The backend is running and responding to ALB health checks. No application errors, no exceptions, no warnings.

#### `/eagle/ecs/frontend-qa`

| Metric | Value |
|--------|-------|
| Error events (24h) | 0 |
| Total activity | **Idle since 2026-03-27T04:17:30Z** |
| Last log stream | `frontend/eagle-frontend/450b680c31bf4b62...` |

The frontend container's last log event was ~2 days ago. This could indicate the ECS task was stopped/replaced or simply has no incoming requests generating logs.

#### `/eagle/app` (shared)

| Metric | Value |
|--------|-------|
| Error events (24h) | 0 |
| Total activity | **Idle since 2026-03-18T02:27:16Z** |
| Last log stream | `session/32342a16-7297-4aa5-aff7-061b44338afa` |

No session-level application logs in 11 days. This log group captures agent session activity and confirms no user-driven conversations are happening in QA.

### Langfuse Trace Errors

| Metric | Value |
|--------|-------|
| Total traces (24h) | 0 |
| Error traces | 0 |
| Unique users | 0 |
| Total cost | $0.00 |
| Avg latency | N/A |

**Note**: QA-specific Langfuse keys (`LANGFUSE_QA_*`) are not configured. The query fell back to dev Langfuse credentials, which is why zero QA-environment traces were found. This is an expected gap — QA traces are either not instrumented or routed to the same Langfuse project as dev.

## Cross-Reference Analysis

### Session Correlation Map

No sessions to correlate — zero feedback, zero errors, zero traces.

### Error Pattern Clusters

No error patterns detected across any source.

### Trend Analysis

| Signal | Trend |
|--------|-------|
| Backend health | Stable — continuous health check 200s |
| Frontend activity | Declining — last activity 2 days ago |
| App session activity | Inactive — last activity 11 days ago |
| User feedback | None recorded for QA tenant |
| Langfuse observability | Not configured for QA (missing `LANGFUSE_QA_*` keys) |

The QA environment appears to be in a "deployed but unused" state. While the absence of errors is positive, the absence of any testing activity is itself a risk — regressions introduced since the last dev triage (2026-03-28) are not being validated in QA.

## Prioritized Issue List

| # | Issue | Severity | Sources | Impact |
|---|-------|----------|---------|--------|
| 1 | Frontend-qa idle for 2+ days | P2 | CW | QA coverage gap — UI regressions undetected |
| 2 | /eagle/app log group inactive 11 days | P2 | CW | No session-level QA testing occurring |
| 3 | Missing LANGFUSE_QA_* credentials | P2 | Config | QA observability blind spot |
| 4 | CloudWatch Log Insights IAM gap | P3 | IAM | Deploy role lacks `logs:StartQuery` — limits triage automation |

## Noise Report

| Pattern | Count | Classification |
|---------|-------|---------------|
| OTel context detach | 0 | N/A |
| Deprecation warnings | 0 | N/A |
| Bedrock cold starts | 0 | N/A |

No noise to filter — the environment is completely quiet.

## Infrastructure Notes

- **CloudWatch Log Insights** returned `AccessDeniedException` for all 3 log groups. The `eagle-deploy-role-dev` IAM role used by GitHub Actions lacks the `logs:StartQuery` / `logs:GetQueryResults` permissions. The `FilterLogEvents` API worked as a fallback.
- **Langfuse** has no QA-specific environment variables (`LANGFUSE_QA_PUBLIC_KEY`, `LANGFUSE_QA_SECRET_KEY`, `LANGFUSE_QA_PROJECT_ID`). The fallback to dev keys means QA traces are either mixed with dev or not captured at all.
