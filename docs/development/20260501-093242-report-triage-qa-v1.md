# EAGLE Triage Report

**Date**: 2026-05-01
**Environment**: qa
**Window**: 24h
**Tenant**: default-qa
**Mode**: Full
**Sources**: DynamoDB Feedback, CloudWatch Logs (qa), Langfuse Traces (skipped — QA keys not configured)

## Executive Summary

The QA environment is largely healthy with one actionable application bug: the `manage_templates` tool's `resolve` action crashes due to an argument-order mismatch when calling `resolve_template()`. This is a deterministic failure — every invocation of the resolve action will TypeError. No user feedback was submitted in the window, and no frontend or app-level errors were logged. Six model cold-start warnings were observed but are non-blocking.

## Source Data

### DynamoDB Feedback

| Metric | Count |
|--------|-------|
| General feedback (bugs, suggestions) | 0 |
| Message-level feedback (thumbs up/down) | 0 |

No feedback items found for tenant `default-qa` in the last 24 hours.

### CloudWatch Errors

#### `/eagle/ecs/backend-qa` — 1 ERROR, 7 WARNINGs

**ERRORS (1)**

| # | Timestamp | Category | Severity | Message |
|---|-----------|----------|----------|---------|
| 1 | 2026-04-30T14:59:58 | Application Bug | ACTIONABLE | `resolve_template() got multiple values for argument 'user_id'` |

**Details — Error #1:**
- **Logger**: `eagle.strands_agent`
- **Tenant**: `default`
- **User**: `24a8d478-20a1-7087-e1a3-56a38d733592`
- **Session**: `5072c54c-c7be-440d-9962-a169959184b4`
- **Stack trace**: `strands_agentic_service.py:4088` → `admin_tools.py:223` → `resolve_template()` TypeError
- **Root cause**: `resolve_template(tenant_id, user_id, doc_type)` is called as `resolve_template(tenant_id, doc_type, user_id=...)` — the positional `doc_type` fills the `user_id` parameter, then `user_id` keyword conflicts.

**WARNINGS (7)**

| # | Timestamp | Category | Severity | Message |
|---|-----------|----------|----------|---------|
| 1 | 2026-05-01T08:18:59 | Model Cold Start | Noise | `keepalive_ping: claude-sonnet-4-6 slow (56.4s)` |
| 2 | 2026-05-01T06:58:31 | Model Cold Start | Noise | `keepalive_ping: claude-sonnet-4-6 slow (8.0s)` |
| 3 | 2026-05-01T06:56:05 | Model Cold Start | Noise | `keepalive_ping: claude-sonnet-4-6 slow (9.7s)` |
| 4 | 2026-05-01T06:45:07 | Model Cold Start | Noise | `keepalive_ping: claude-sonnet-4-6 slow (8.7s)` |
| 5 | 2026-04-30T22:06:10 | Model Cold Start | Noise | `keepalive_ping: claude-sonnet-4-6 slow (8.0s)` |
| 6 | 2026-04-30T21:53:02 | Model Cold Start | Noise | `keepalive_ping: claude-sonnet-4-6 slow (8.1s)` |
| 7 | 2026-04-30T15:10:58 | Configuration | Warning | `jira_client: base_url or api_token not configured, skipping` |

#### `/eagle/ecs/frontend-qa` — 0 errors

No errors or warnings found. 0 records scanned (log group may be empty or inactive).

#### `/eagle/app` — 0 errors

No errors or warnings found. 0 records scanned (shared log group, no matching events in window).

### Langfuse Trace Errors

**Skipped** — `LANGFUSE_QA_PUBLIC_KEY` and `LANGFUSE_QA_SECRET_KEY` are not configured in `server/.env`. QA-specific Langfuse telemetry is unavailable. Dev Langfuse keys exist but would query the wrong project.

**Gap**: Without Langfuse QA data, we cannot correlate trace-level errors, measure latency distributions, or identify cost anomalies for QA sessions. Recommend configuring QA-specific Langfuse keys.

## Cross-Reference Analysis

### Session Correlation Map

| Session ID | Feedback | CloudWatch | Langfuse |
|------------|----------|------------|----------|
| `5072c54c-c7be-440d-9962-a169959184b4` | none | 1 ERROR (resolve_template) | N/A |

No sessions appear in multiple sources. The single error session has no associated feedback, suggesting the user may not have reported the failure.

### Error Pattern Clusters

| Cluster | CloudWatch Signal | Langfuse Signal | Feedback Signal | Assessment |
|---------|------------------|-----------------|-----------------|------------|
| **Application Bug** | `resolve_template() got multiple values for argument 'user_id'` (1x) | N/A | none | Deterministic crash in template resolve path |
| **Model Issues** | `keepalive_ping slow` (6x, 8-56s) | N/A | none | Bedrock cold starts; existing keepalive mitigation is partially effective |
| **Configuration** | `jira_client not configured` (1x) | N/A | none | Expected in QA if Jira integration not provisioned |

### Trend Analysis

- **resolve_template bug**: Single occurrence, but deterministic — will recur on every `manage_templates(action="resolve")` call. This is a latent bug introduced by parameter-order inconsistency between `resolve_template` and `delete_template`.
- **Cold starts**: 6 occurrences over 24h clustered in early morning UTC (06:45–08:18), consistent with low-traffic cold-start pattern. One outlier at 56.4s; remaining 5 are 8–10s. The keepalive mechanism is running but not fully preventing cold starts.
- **No frontend errors**: Frontend-qa is stable or has minimal traffic.
- **No user feedback**: Either QA has low usage or the feedback mechanism is not actively used in QA.

## Prioritized Issue List

| # | Issue | Composite Score | Priority | Sources | Sessions | Deterministic |
|---|-------|-----------------|----------|---------|----------|---------------|
| 1 | `resolve_template()` argument-order mismatch in `admin_tools.py:223` | 3 | **P2** | CW | 1 | Yes — every resolve call crashes |
| 2 | Bedrock model cold starts (keepalive_ping slow, 8-56s) | 1 | P3 | CW | 0 | No — intermittent |
| 3 | Jira client not configured in QA | 0 | P3 | CW | 0 | Expected |
| 4 | Langfuse QA keys not configured | 0 | P3 | Config | 0 | Observability gap |

**Scoring rationale for #1**: Raw composite score is 1 (1 occurrence, single source, no feedback, ACTIONABLE). Elevated to P2 because the bug is deterministic — the `manage_templates` resolve action is 100% broken. Any user attempting template resolution via the agent tool will hit this crash.

## Noise Report

| Category | Count | Justification |
|----------|-------|---------------|
| Bedrock cold-start keepalive warnings | 6 | Known pattern — `ModelNotReadyException` / cold-start variant. Keepalive is active but cannot fully prevent Bedrock-side cold starts. Non-blocking. |
| Jira client config warning | 1 | Expected in QA when Jira integration credentials are not provisioned. Logged at WARNING, not ERROR. Non-blocking. |
