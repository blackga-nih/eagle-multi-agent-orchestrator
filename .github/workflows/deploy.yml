name: Deploy EAGLE Platform

on:
  push:
    branches: [main]
  workflow_dispatch:
    inputs:
      environment:
        description: 'Target environment'
        required: true
        default: 'dev'
        type: choice
        options:
          - dev
          - qa
      deploy_infra:
        description: 'Deploy CDK infrastructure'
        required: false
        default: 'true'
        type: string
      deploy_app:
        description: 'Build and deploy application'
        required: false
        default: 'true'
        type: string
      deploy_mode:
        description: 'Deploy mode (auto=detect from paths, full=all gates, mini=lint only)'
        required: false
        default: 'auto'
        type: choice
        options:
          - auto
          - full
          - mini
      run_eval:
        description: 'Run eval suite (expensive — off by default)'
        required: false
        default: 'false'
        type: choice
        options:
          - 'false'
          - 'true'
      eval_tests:
        description: 'Comma-separated test numbers when run_eval=true (e.g., 1,2,3 or 16,17,18,19,20). REQUIRED when run_eval=true — never runs all.'
        required: false
        default: ''
        type: string

concurrency:
  group: deploy-${{ github.event_name == 'workflow_dispatch' && inputs.environment || 'dev' }}-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: read
  id-token: write

env:
  AWS_REGION: us-east-1
  CDK_DIR: infrastructure/cdk-eagle
  # Model selection managed by circuit breaker in strands_agentic_service.py.
  # Chain: Sonnet 4.6 → 4.5 → 4.0 → Haiku (auto-fallback on failures).
  # Set EAGLE_BEDROCK_MODEL_ID to override the primary (position 0) model.
  # EAGLE_BEDROCK_MODEL_ID: us.anthropic.claude-sonnet-4-6

jobs:
  # ── Resolve environment-specific variables ───────────────────
  setup:
    runs-on: ubuntu-latest
    outputs:
      env_name: ${{ steps.vars.outputs.env_name }}
      cluster_name: ${{ steps.vars.outputs.cluster_name }}
      backend_service: ${{ steps.vars.outputs.backend_service }}
      frontend_service: ${{ steps.vars.outputs.frontend_service }}
      backend_ecr: ${{ steps.vars.outputs.backend_ecr }}
      frontend_ecr: ${{ steps.vars.outputs.frontend_ecr }}
      compute_stack: ${{ steps.vars.outputs.compute_stack }}
      frontend_tg_arn: ${{ steps.vars.outputs.frontend_tg_arn }}
    steps:
    - name: Resolve environment variables
      id: vars
      run: |
        if [ "${{ github.event_name }}" = "workflow_dispatch" ]; then
          ENV="${{ inputs.environment }}"
        else
          ENV="dev"
        fi
        echo "env_name=$ENV" >> $GITHUB_OUTPUT
        echo "cluster_name=eagle-${ENV}" >> $GITHUB_OUTPUT
        echo "backend_service=eagle-backend-${ENV}" >> $GITHUB_OUTPUT
        echo "frontend_service=eagle-frontend-${ENV}" >> $GITHUB_OUTPUT
        echo "backend_ecr=eagle-backend-${ENV}" >> $GITHUB_OUTPUT
        echo "frontend_ecr=eagle-frontend-${ENV}" >> $GITHUB_OUTPUT

        if [ "$ENV" = "qa" ]; then
          echo "compute_stack=EagleComputeStackQA" >> $GITHUB_OUTPUT
          echo "frontend_tg_arn=arn:aws:elasticloadbalancing:us-east-1:695681773636:targetgroup/EAGLE-QA-FRONTEND-IP/152273fb8d5cd028" >> $GITHUB_OUTPUT
        else
          echo "compute_stack=EagleComputeStack" >> $GITHUB_OUTPUT
          echo "frontend_tg_arn=arn:aws:elasticloadbalancing:us-east-1:695681773636:targetgroup/EAGLE-DEV-FRONTEND-IP/0e76ba2b0ab49d0c" >> $GITHUB_OUTPUT
        fi

        echo "Environment: $ENV"

  # ── Detect changed paths + deploy mode ─────────────────────
  changes:
    needs: setup
    runs-on: ubuntu-latest
    outputs:
      infra: ${{ steps.filter.outputs.infra }}
      backend: ${{ steps.filter.outputs.backend }}
      frontend: ${{ steps.filter.outputs.frontend }}
      significant: ${{ steps.filter.outputs.significant }}
      deploy_mode: ${{ steps.mode.outputs.deploy_mode }}
    steps:
    - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd  # v6.0.2
    - uses: dorny/paths-filter@fbd0ab8f3e69293af611ebaee6363fc25e6d187d  # v4.0.1
      id: filter
      with:
        filters: |
          infra:
            - 'infrastructure/**'
          backend:
            - 'server/**'
            - 'eagle-plugin/**'
            - 'deployment/docker/Dockerfile.backend'
            - 'deployment/docker-compose*.yml'
          frontend:
            - 'client/**'
            - 'deployment/docker/Dockerfile.frontend'
          significant:
            - 'server/app/**'
            - 'server/tests/**'
            - 'eagle-plugin/**'
            - 'infrastructure/**'
            - 'client/components/**'
            - 'client/hooks/**'
            - 'client/lib/**'
            - 'client/app/api/**'
            - 'deployment/**'

    - name: Determine deploy mode
      id: mode
      run: |
        ENV="${{ needs.setup.outputs.env_name }}"
        INPUT_MODE="${{ inputs.deploy_mode }}"
        # QA always uses mini — code was already tested on DEV
        if [ "$ENV" = "qa" ]; then
          echo "deploy_mode=mini" >> $GITHUB_OUTPUT
          echo "Deploy mode: mini (QA — skip test gates)"
        elif [ "$INPUT_MODE" = "full" ] || [ "$INPUT_MODE" = "mini" ]; then
          echo "deploy_mode=$INPUT_MODE" >> $GITHUB_OUTPUT
          echo "Deploy mode: $INPUT_MODE (manual override)"
        elif [ "${{ steps.filter.outputs.significant }}" = "true" ]; then
          echo "deploy_mode=full" >> $GITHUB_OUTPUT
          echo "Deploy mode: full (significant changes detected)"
        else
          echo "deploy_mode=mini" >> $GITHUB_OUTPUT
          echo "Deploy mode: mini (non-significant changes only)"
        fi

  # ── L1: Lint (both modes) ──────────────────────────────────
  lint:
    needs: [setup, changes]
    runs-on: ubuntu-latest
    outputs:
      ruff_result: ${{ steps.ruff.outcome }}
      tsc_result: ${{ steps.tsc.outcome }}
      detail: ${{ steps.summary.outputs.detail }}
    steps:
    - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd  # v6.0.2

    - name: Setup Python 3.11
      uses: actions/setup-python@a309ff8b426b58ec0e2a45f0f869d46889d02405  # v6.2.0
      with:
        python-version: '3.11'

    - name: Install ruff
      run: pip install ruff

    - name: Ruff lint
      id: ruff
      run: cd server && ruff check app/ 2>&1

    - name: Setup Node.js
      uses: actions/setup-node@53b83947a5a98c8d113130e565377fae1a50d02f  # v6.3.0
      with:
        node-version: '20'
        cache: 'npm'
        cache-dependency-path: client/package-lock.json

    - name: Install client dependencies
      run: cd client && npm ci

    - name: TypeScript check
      id: tsc
      run: cd client && npx tsc --noEmit 2>&1

    - name: Summarize
      id: summary
      if: always()
      run: |
        RUFF="${{ steps.ruff.outcome }}"
        TSC="${{ steps.tsc.outcome }}"
        echo "detail=ruff: $RUFF, tsc: $TSC" >> $GITHUB_OUTPUT

  # ── L2: Unit Tests (full only) ─────────────────────────────
  unit-tests:
    needs: [changes, lint]
    if: needs.changes.outputs.deploy_mode == 'full'
    runs-on: ubuntu-latest
    outputs:
      test_result: ${{ steps.pytest.outcome }}
      detail: ${{ steps.parse.outputs.detail }}
    steps:
    - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd  # v6.0.2

    - name: Setup Python 3.11
      uses: actions/setup-python@a309ff8b426b58ec0e2a45f0f869d46889d02405  # v6.2.0
      with:
        python-version: '3.11'
        cache: 'pip'
        cache-dependency-path: server/requirements.txt

    - name: Configure AWS credentials (OIDC)
      uses: aws-actions/configure-aws-credentials@8df5847569e6427dd6c4fb1cf565c83acfa8afa7  # v6.0.0
      with:
        role-to-assume: ${{ secrets.DEPLOY_ROLE_ARN }}
        aws-region: ${{ env.AWS_REGION }}

    - name: Install dependencies
      run: pip install -r server/requirements.txt pytest pytest-asyncio

    - name: Run unit tests
      id: pytest
      env:
        # Use Haiku for CI unit tests (fast, cheap). Circuit breaker manages prod.
        EAGLE_BEDROCK_MODEL_ID: us.anthropic.claude-haiku-4-5-20251001-v1:0
        EAGLE_CB_STARTUP_PROBE: "false"
      run: |
        set -o pipefail
        cd server
        python -m pytest tests/ -v \
          --ignore=tests/test_strands_eval.py \
          --ignore=tests/test_eagle_sdk_eval.py \
          --ignore=tests/test_strands_poc.py \
          --ignore=tests/test_strands_multi_agent.py \
          --ignore=tests/test_feedback_auth.py \
          --junitxml=test-results.xml \
          2>&1 | tee pytest-output.txt

    - name: Parse results
      id: parse
      if: always()
      run: |
        if [ -f server/pytest-output.txt ]; then
          SUMMARY=$(tail -1 server/pytest-output.txt)
          echo "detail=$SUMMARY" >> $GITHUB_OUTPUT
        else
          echo "detail=no output" >> $GITHUB_OUTPUT
        fi

    - name: Upload test results
      if: always()
      uses: actions/upload-artifact@bbbca2ddaa5d8feaa63e36b76fdaad77386f024f  # v7.0.0
      with:
        name: unit-test-results-${{ github.sha }}
        path: server/test-results.xml
        retention-days: 14
        if-no-files-found: warn

  # ── L4: CDK Synth (full only, parallel with L2) ────────────
  cdk-synth:
    needs: changes
    if: needs.changes.outputs.deploy_mode == 'full'
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd  # v6.0.2

    - name: Configure AWS credentials (OIDC)
      uses: aws-actions/configure-aws-credentials@8df5847569e6427dd6c4fb1cf565c83acfa8afa7  # v6.0.0
      with:
        role-to-assume: ${{ secrets.DEPLOY_ROLE_ARN }}
        aws-region: ${{ env.AWS_REGION }}

    - name: Setup Node.js
      uses: actions/setup-node@53b83947a5a98c8d113130e565377fae1a50d02f  # v6.3.0
      with:
        node-version: '20'
        cache: 'npm'
        cache-dependency-path: infrastructure/cdk-eagle/package-lock.json

    - name: Install CDK dependencies
      run: cd ${{ env.CDK_DIR }} && npm ci

    - name: CDK Synth
      env:
        # Microsoft Entra OIDC config + CBIIT external ALB hostnames per env.
        # Stored as GitHub Actions repo variables (gh variable list).
        EAGLE_ENTRA_TENANT_ID: ${{ vars.EAGLE_ENTRA_TENANT_ID }}
        EAGLE_ENTRA_CLIENT_ID: ${{ vars.EAGLE_ENTRA_CLIENT_ID }}
        EAGLE_ENTRA_REDIRECT_URI: ${{ vars.EAGLE_ENTRA_REDIRECT_URI }}
        EAGLE_ENTRA_REDIRECT_URI_QA: ${{ vars.EAGLE_ENTRA_REDIRECT_URI_QA }}
        EAGLE_EXTERNAL_HOSTNAME_DEV: ${{ vars.EAGLE_EXTERNAL_HOSTNAME_DEV }}
        EAGLE_EXTERNAL_HOSTNAME_QA: ${{ vars.EAGLE_EXTERNAL_HOSTNAME_QA }}
      run: |
        ACCOUNT=$(aws sts get-caller-identity --query Account --output text)
        export CDK_DEFAULT_ACCOUNT=$ACCOUNT
        export CDK_DEFAULT_REGION=${{ env.AWS_REGION }}
        cd ${{ env.CDK_DIR }} && npx cdk synth --quiet

  # ── L5: Integration — uvicorn health check (full only) ─────
  integration:
    needs: [changes, unit-tests, cdk-synth]
    if: |
      always() &&
      needs.changes.outputs.deploy_mode == 'full' &&
      needs.unit-tests.result == 'success' &&
      needs.cdk-synth.result == 'success'
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd  # v6.0.2

    - name: Configure AWS credentials (OIDC)
      uses: aws-actions/configure-aws-credentials@8df5847569e6427dd6c4fb1cf565c83acfa8afa7  # v6.0.0
      with:
        role-to-assume: ${{ secrets.DEPLOY_ROLE_ARN }}
        aws-region: ${{ env.AWS_REGION }}

    - name: Setup Python 3.11
      uses: actions/setup-python@a309ff8b426b58ec0e2a45f0f869d46889d02405  # v6.2.0
      with:
        python-version: '3.11'
        cache: 'pip'
        cache-dependency-path: server/requirements.txt

    - name: Install dependencies
      run: pip install -r server/requirements.txt

    - name: Start uvicorn and health check
      run: |
        cd server
        python -m uvicorn app.main:app --host 127.0.0.1 --port 8000 &
        SERVER_PID=$!

        echo "Waiting for backend (PID $SERVER_PID)..."
        for i in $(seq 1 30); do
          if curl -sf http://127.0.0.1:8000/api/health > /tmp/health.json 2>/dev/null; then
            STATUS=$(python3 -c "import json; print(json.load(open('/tmp/health.json'))['status'])")
            if [ "$STATUS" = "healthy" ]; then
              echo "L5 PASS: backend healthy after ${i} attempts"
              kill $SERVER_PID 2>/dev/null || true
              exit 0
            fi
          fi
          echo "  [$i/30] not ready..."
          sleep 2
        done

        echo "L5 FAIL: backend did not become healthy"
        kill $SERVER_PID 2>/dev/null || true
        exit 1

  # ── L6: Eval — Haiku lightweight gate (full only) ──────────
  eval:
    needs: [changes, integration]
    if: |
      always() &&
      needs.changes.outputs.deploy_mode == 'full' &&
      needs.integration.result == 'success' &&
      inputs.run_eval == 'true'
    runs-on: ubuntu-latest
    timeout-minutes: 15
    outputs:
      eval_detail: ${{ steps.parse.outputs.eval_detail }}
      mvp1_detail: ${{ steps.parse.outputs.mvp1_detail }}
    steps:
    - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd  # v6.0.2

    - name: Configure AWS credentials (OIDC)
      uses: aws-actions/configure-aws-credentials@8df5847569e6427dd6c4fb1cf565c83acfa8afa7  # v6.0.0
      with:
        role-to-assume: ${{ secrets.DEPLOY_ROLE_ARN }}
        aws-region: ${{ env.AWS_REGION }}

    - name: Setup Python 3.11
      uses: actions/setup-python@a309ff8b426b58ec0e2a45f0f869d46889d02405  # v6.2.0
      with:
        python-version: '3.11'
        cache: 'pip'
        cache-dependency-path: server/requirements.txt

    - name: Install dependencies
      run: pip install -r server/requirements.txt

    - name: Derive S3 bucket names
      id: s3
      run: |
        ACCOUNT_ID=$(aws sts get-caller-identity --query 'Account' --output text)
        echo "document-bucket=eagle-documents-${ACCOUNT_ID}-dev" >> $GITHUB_OUTPUT
        echo "eval-bucket=eagle-eval-artifacts-${ACCOUNT_ID}-dev" >> $GITHUB_OUTPUT

    - name: Guard — require eval_tests input
      run: |
        if [ -z "${{ inputs.eval_tests }}" ]; then
          echo "ERROR: run_eval=true but eval_tests input is empty."
          echo "Provide comma-separated test numbers (e.g., 1,2,3 or 16,17,18,19,20)."
          echo "Refusing to run all tests by default (cost control)."
          exit 1
        fi
        echo "Running eval tests: ${{ inputs.eval_tests }}"

    - name: Run eval (input-driven)
      id: eval
      env:
        USE_BEDROCK: 'true'
        CLAUDE_CODE_USE_BEDROCK: '1'
        EAGLE_SESSIONS_TABLE: eagle
        S3_BUCKET: ${{ steps.s3.outputs.document-bucket }}
        EVAL_BUCKET: ${{ steps.s3.outputs.eval-bucket }}
        LANGFUSE_PUBLIC_KEY: ${{ secrets.LANGFUSE_PUBLIC_KEY }}
        LANGFUSE_SECRET_KEY: ${{ secrets.LANGFUSE_SECRET_KEY }}
        LANGFUSE_HOST: ${{ secrets.LANGFUSE_HOST }}
      run: |
        cd server
        python tests/test_strands_eval.py \
          --tests "${{ inputs.eval_tests }}" \
          --model us.anthropic.claude-3-5-haiku-20241022-v1:0 \
          2>&1 | tee eval-results.txt

    - name: Parse eval results
      id: parse
      if: always()
      run: |
        DETAIL="unknown"
        if [ -f server/eval-results.txt ]; then
          DETAIL=$(tail -3 server/eval-results.txt | head -1)
        fi
        # Preserve both output names for backwards compat with downstream consumers
        echo "eval_detail=$DETAIL" >> $GITHUB_OUTPUT
        echo "mvp1_detail=$DETAIL" >> $GITHUB_OUTPUT

    - name: Upload eval artifacts
      if: always()
      uses: actions/upload-artifact@bbbca2ddaa5d8feaa63e36b76fdaad77386f024f  # v7.0.0
      with:
        name: eval-results-${{ github.sha }}
        path: server/eval-results.txt
        retention-days: 30
        if-no-files-found: warn

  # ── Deploy CDK Infrastructure ──────────────────────────────
  deploy-infra:
    needs: [setup, changes, lint, unit-tests, integration, eval]
    runs-on: ubuntu-latest
    if: |
      always() &&
      needs.lint.result == 'success' &&
      (needs.changes.outputs.deploy_mode == 'mini' || needs.eval.result == 'success' || (needs.eval.result == 'skipped' && needs.integration.result != 'failure' && needs.unit-tests.result != 'failure')) &&
      (
        (github.event_name == 'push' && needs.changes.outputs.infra == 'true') ||
        (github.event_name == 'workflow_dispatch' && inputs.deploy_infra == 'true')
      )

    steps:
    - name: Checkout code
      uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd  # v6.0.2

    - name: Configure AWS credentials (OIDC)
      uses: aws-actions/configure-aws-credentials@8df5847569e6427dd6c4fb1cf565c83acfa8afa7  # v6.0.0
      with:
        role-to-assume: ${{ secrets.DEPLOY_ROLE_ARN }}
        aws-region: ${{ env.AWS_REGION }}

    - name: Setup Node.js
      uses: actions/setup-node@53b83947a5a98c8d113130e565377fae1a50d02f  # v6.3.0
      with:
        node-version: '20'
        cache: 'npm'
        cache-dependency-path: infrastructure/cdk-eagle/package-lock.json

    - name: Install CDK dependencies
      run: cd ${{ env.CDK_DIR }} && npm ci

    - name: CDK Deploy
      env:
        # Same Entra/hostname vars as CDK Synth — `cdk deploy` re-synthesizes,
        # so they must be present here too or the task def lands with empty
        # values.
        EAGLE_ENTRA_TENANT_ID: ${{ vars.EAGLE_ENTRA_TENANT_ID }}
        EAGLE_ENTRA_CLIENT_ID: ${{ vars.EAGLE_ENTRA_CLIENT_ID }}
        EAGLE_ENTRA_REDIRECT_URI: ${{ vars.EAGLE_ENTRA_REDIRECT_URI }}
        EAGLE_ENTRA_REDIRECT_URI_QA: ${{ vars.EAGLE_ENTRA_REDIRECT_URI_QA }}
        EAGLE_EXTERNAL_HOSTNAME_DEV: ${{ vars.EAGLE_EXTERNAL_HOSTNAME_DEV }}
        EAGLE_EXTERNAL_HOSTNAME_QA: ${{ vars.EAGLE_EXTERNAL_HOSTNAME_QA }}
      run: |
        cd ${{ env.CDK_DIR }}
        # EagleComputeStack carries the dev ECS task definition (env vars,
        # CDK-managed image, IAM linkage). Excluding it means env-var changes
        # added in CDK never reach the running task — discovered 2026-04-30
        # when S3_VECTORS_BUCKET / S3_VECTORS_INDEX added in ff8654f never
        # deployed, leaving vectors search pointing at the decommissioned
        # rh-eagle bucket and silently AccessDenied'ing.
        # Two-phase deploy so cross-stack CFN export removals work:
        # Phase 1 — BOTH Compute stacks (dev + qa) so their templates drop
        # ImportValue references to exports Core is about to delete. This
        # bit the Cognito → Entra migration on 2026-05-13: CFN refused to
        # remove UserPool exports while Compute still imported them. And
        # again on 2026-05-14 when only EagleComputeStack was in Phase 1
        # on dev deploys — EagleComputeStackQA still imported the exports,
        # so Core could not drop them. Both must be updated in lockstep
        # because they share Core regardless of which env we're targeting.
        # Phase 2 — Core + remaining stacks (export deletion is safe now).
        COMPUTE_STACKS="EagleComputeStack EagleComputeStackQA"
        for s in $COMPUTE_STACKS; do
          npx cdk deploy "$s" --exclusively --require-approval never
        done
        npx cdk deploy EagleCiCdStack EagleStorageStack EagleCoreStack EagleEvalStack EagleBackupStack \
          --exclusively --require-approval never --outputs-file outputs.json

  # ── Build & Push Backend Container ─────────────────────────
  deploy-backend:
    needs: [setup, changes, lint, unit-tests, integration, eval, deploy-infra]
    runs-on: ubuntu-latest
    if: |
      always() &&
      needs.lint.result == 'success' &&
      (needs.changes.outputs.deploy_mode == 'mini' || needs.eval.result == 'success' || (needs.eval.result == 'skipped' && needs.integration.result != 'failure' && needs.unit-tests.result != 'failure')) &&
      needs.deploy-infra.result != 'failure' &&
      (
        (github.event_name == 'push' && needs.changes.outputs.backend == 'true') ||
        (github.event_name == 'workflow_dispatch' && inputs.deploy_app == 'true')
      )

    steps:
    - name: Checkout code
      uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd  # v6.0.2

    - name: Configure AWS credentials (OIDC)
      uses: aws-actions/configure-aws-credentials@8df5847569e6427dd6c4fb1cf565c83acfa8afa7  # v6.0.0
      with:
        role-to-assume: ${{ secrets.DEPLOY_ROLE_ARN }}
        aws-region: ${{ env.AWS_REGION }}

    - name: Derive ECR URI
      id: ecr
      run: |
        ACCOUNT=$(aws sts get-caller-identity --query Account --output text)
        echo "uri=${ACCOUNT}.dkr.ecr.us-east-1.amazonaws.com/${{ needs.setup.outputs.backend_ecr }}" >> $GITHUB_OUTPUT

    - name: Login to ECR
      uses: aws-actions/amazon-ecr-login@183a1442edf41672e66566b7fc560e297a290896  # v2.1.1

    - name: Set up Docker Buildx
      uses: docker/setup-buildx-action@v3

    - name: Build and push backend image
      uses: docker/build-push-action@v5
      with:
        context: .
        file: deployment/docker/Dockerfile.backend
        push: true
        tags: |
          ${{ steps.ecr.outputs.uri }}:${{ github.sha }}
          ${{ steps.ecr.outputs.uri }}:latest
        build-args: |
          GIT_SHA=${{ github.sha }}
        cache-from: type=gha,scope=backend
        cache-to: type=gha,mode=max,scope=backend

    - name: Update task definition with Bedrock model ID and Langfuse credentials
      id: task-def
      env:
        LANGFUSE_PUBLIC_KEY: ${{ secrets.LANGFUSE_PUBLIC_KEY }}
        LANGFUSE_SECRET_KEY: ${{ secrets.LANGFUSE_SECRET_KEY }}
        LANGFUSE_HOST: ${{ secrets.LANGFUSE_HOST }}
      run: |
        # Fetch current task def, strip read-only fields, inject env vars
        aws ecs describe-task-definition \
          --task-definition ${{ needs.setup.outputs.backend_service }} \
          --region ${{ env.AWS_REGION }} \
          --query 'taskDefinition' --output json > /tmp/current_td.json

        python3 - <<'PYEOF'
        import json, os

        with open('/tmp/current_td.json') as f:
            td = json.load(f)

        for key in ['taskDefinitionArn','revision','status','requiresAttributes',
                    'placementConstraints','compatibilities','registeredAt','registeredBy']:
            td.pop(key, None)

        env = td['containerDefinitions'][0].get('environment', [])
        # Model selection managed by circuit breaker — remove any stale override
        env = [e for e in env if e['name'] != 'EAGLE_BEDROCK_MODEL_ID']

        # Inject Langfuse credentials from GitHub Secrets
        for var_name in ['LANGFUSE_PUBLIC_KEY', 'LANGFUSE_SECRET_KEY', 'LANGFUSE_HOST']:
            env = [e for e in env if e['name'] != var_name]
            val = os.environ.get(var_name, '')
            if val:
                env.append({'name': var_name, 'value': val})

        td['containerDefinitions'][0]['environment'] = env

        # Pin image to this commit's SHA (not implicit :latest)
        td['containerDefinitions'][0]['image'] = '${{ steps.ecr.outputs.uri }}:${{ github.sha }}'

        with open('/tmp/new_td.json', 'w') as f:
            json.dump(td, f)
        print("Task def updated: image=${{ steps.ecr.outputs.uri }}:${{ github.sha }}, model=circuit-breaker-managed")
        PYEOF

        NEW_TD_ARN=$(aws ecs register-task-definition \
          --cli-input-json file:///tmp/new_td.json \
          --region ${{ env.AWS_REGION }} \
          --query 'taskDefinition.taskDefinitionArn' --output text)

        echo "task-def-arn=$NEW_TD_ARN" >> $GITHUB_OUTPUT
        echo "Registered: $NEW_TD_ARN"

    - name: Update ECS backend service
      run: |
        aws ecs update-service \
          --cluster ${{ needs.setup.outputs.cluster_name }} \
          --service ${{ needs.setup.outputs.backend_service }} \
          --task-definition ${{ steps.task-def.outputs.task-def-arn }} \
          --force-new-deployment \
          --region ${{ env.AWS_REGION }}

    - name: Wait for backend deployment
      run: |
        aws ecs wait services-stable \
          --cluster ${{ needs.setup.outputs.cluster_name }} \
          --services ${{ needs.setup.outputs.backend_service }} \
          --region ${{ env.AWS_REGION }}
        echo "Backend deployment stable"

  # ── Build & Push Frontend Container ────────────────────────
  deploy-frontend:
    needs: [setup, changes, lint, unit-tests, integration, eval, deploy-infra]
    runs-on: ubuntu-latest
    if: |
      always() &&
      needs.lint.result == 'success' &&
      (needs.changes.outputs.deploy_mode == 'mini' || needs.eval.result == 'success' || (needs.eval.result == 'skipped' && needs.integration.result != 'failure' && needs.unit-tests.result != 'failure')) &&
      needs.deploy-infra.result != 'failure' &&
      (
        (github.event_name == 'push' && needs.changes.outputs.frontend == 'true') ||
        (github.event_name == 'workflow_dispatch' && inputs.deploy_app == 'true')
      )

    steps:
    - name: Checkout code
      uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd  # v6.0.2

    - name: Configure AWS credentials (OIDC)
      uses: aws-actions/configure-aws-credentials@8df5847569e6427dd6c4fb1cf565c83acfa8afa7  # v6.0.0
      with:
        role-to-assume: ${{ secrets.DEPLOY_ROLE_ARN }}
        aws-region: ${{ env.AWS_REGION }}

    - name: Derive ECR URI and backend URL for frontend build
      id: config
      run: |
        ACCOUNT=$(aws sts get-caller-identity --query Account --output text)
        echo "uri=${ACCOUNT}.dkr.ecr.us-east-1.amazonaws.com/${{ needs.setup.outputs.frontend_ecr }}" >> $GITHUB_OUTPUT

        # Backend ALB URL is needed at BUILD time, not just runtime — Next.js
        # standalone output bakes rewrites() into routes-manifest.json at
        # `next build`. Without this, /api/auth/* rewrites resolve to
        # localhost:8000 in the baked manifest and the Entra callback
        # cannot reach FastAPI (root cause of 2026-05-14 dev failure).
        STACK_NAME="EagleComputeStack"
        if [ "${{ needs.setup.outputs.env_name }}" = "qa" ]; then
          STACK_NAME="EagleComputeStackQA"
        fi
        BACKEND_URL=$(aws cloudformation describe-stacks --stack-name "$STACK_NAME" \
          --query "Stacks[0].Outputs[?OutputKey=='BackendUrl'].OutputValue" --output text)
        echo "backend-url=$BACKEND_URL" >> $GITHUB_OUTPUT
        echo "Backend URL for frontend build: $BACKEND_URL"

    - name: Login to ECR
      uses: aws-actions/amazon-ecr-login@183a1442edf41672e66566b7fc560e297a290896  # v2.1.1

    - name: Set up Docker Buildx
      uses: docker/setup-buildx-action@v3

    - name: Build and push frontend image
      uses: docker/build-push-action@v5
      with:
        context: .
        file: deployment/docker/Dockerfile.frontend
        push: true
        tags: |
          ${{ steps.config.outputs.uri }}:${{ github.sha }}
          ${{ steps.config.outputs.uri }}:latest
        build-args: |
          FASTAPI_URL=${{ steps.config.outputs.backend-url }}
        cache-from: type=gha,scope=frontend
        cache-to: type=gha,mode=max,scope=frontend

    - name: Update frontend task definition with new image
      id: task-def
      run: |
        aws ecs describe-task-definition \
          --task-definition ${{ needs.setup.outputs.frontend_service }} \
          --region ${{ env.AWS_REGION }} \
          --query 'taskDefinition' --output json > /tmp/current_td.json

        python3 - <<'PYEOF'
        import json
        with open('/tmp/current_td.json') as f:
            td = json.load(f)
        for key in ['taskDefinitionArn','revision','status','requiresAttributes',
                    'placementConstraints','compatibilities','registeredAt','registeredBy']:
            td.pop(key, None)
        td['containerDefinitions'][0]['image'] = '${{ steps.config.outputs.uri }}:${{ github.sha }}'
        with open('/tmp/new_td.json', 'w') as f:
            json.dump(td, f)
        print("Frontend task def updated: image=${{ steps.config.outputs.uri }}:${{ github.sha }}")
        PYEOF

        NEW_TD_ARN=$(aws ecs register-task-definition \
          --cli-input-json file:///tmp/new_td.json \
          --region ${{ env.AWS_REGION }} \
          --query 'taskDefinition.taskDefinitionArn' --output text)
        echo "task-def-arn=$NEW_TD_ARN" >> $GITHUB_OUTPUT
        echo "Registered: $NEW_TD_ARN"

    - name: Update ECS frontend service
      run: |
        aws ecs update-service \
          --cluster ${{ needs.setup.outputs.cluster_name }} \
          --service ${{ needs.setup.outputs.frontend_service }} \
          --task-definition ${{ steps.task-def.outputs.task-def-arn }} \
          --force-new-deployment \
          --region ${{ env.AWS_REGION }}

    - name: Wait for frontend deployment
      run: |
        aws ecs wait services-stable \
          --cluster ${{ needs.setup.outputs.cluster_name }} \
          --services ${{ needs.setup.outputs.frontend_service }} \
          --region ${{ env.AWS_REGION }}
        echo "Frontend deployment stable"

    - name: Sync ALB target group to new task IP
      run: |
        TG_ARN="${{ needs.setup.outputs.frontend_tg_arn }}"

        TASK_ARN=$(aws ecs list-tasks \
          --cluster ${{ needs.setup.outputs.cluster_name }} \
          --service-name ${{ needs.setup.outputs.frontend_service }} \
          --desired-status RUNNING \
          --region ${{ env.AWS_REGION }} \
          --query 'taskArns[0]' --output text)

        NEW_IP=$(aws ecs describe-tasks \
          --cluster ${{ needs.setup.outputs.cluster_name }} \
          --tasks "$TASK_ARN" \
          --region ${{ env.AWS_REGION }} \
          --query 'tasks[0].attachments[0].details[?name==`privateIPv4Address`].value' \
          --output text)

        echo "New frontend task IP: $NEW_IP"

        OLD_TARGETS=$(aws elbv2 describe-target-health \
          --target-group-arn "$TG_ARN" \
          --region ${{ env.AWS_REGION }} \
          --query 'TargetHealthDescriptions[*].{Id:Target.Id,Port:Target.Port}' \
          --output json)

        echo "$OLD_TARGETS" | jq -c '.[]' | while read row; do
          OLD_IP=$(echo "$row" | jq -r '.Id')
          OLD_PORT=$(echo "$row" | jq -r '.Port')
          if [ "$OLD_IP" != "$NEW_IP" ]; then
            echo "Deregistering old target $OLD_IP:$OLD_PORT"
            aws elbv2 deregister-targets \
              --target-group-arn "$TG_ARN" \
              --targets Id="$OLD_IP",Port="$OLD_PORT" \
              --region ${{ env.AWS_REGION }}
          fi
        done

        aws elbv2 register-targets \
          --target-group-arn "$TG_ARN" \
          --targets Id="$NEW_IP",Port=3000 \
          --region ${{ env.AWS_REGION }}

        echo "${{ needs.setup.outputs.env_name }} ALB now targeting $NEW_IP:3000"

  # ── Report: Teams notification ─────────────────────────────
  report:
    needs: [setup, changes, lint, unit-tests, cdk-synth, integration, eval, deploy-infra, deploy-backend, deploy-frontend]
    if: always()
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd  # v6.0.2

    - name: Setup Python 3.11
      uses: actions/setup-python@a309ff8b426b58ec0e2a45f0f869d46889d02405  # v6.2.0
      with:
        python-version: '3.11'

    - name: Install httpx
      run: pip install httpx

    - name: Send deploy report to Teams
      env:
        TEAMS_WEBHOOK_URL: ${{ secrets.TEAMS_WEBHOOK_URL }}
        EAGLE_ENV: ${{ needs.setup.outputs.env_name }}
        DEPLOY_MODE: ${{ needs.changes.outputs.deploy_mode }}
        COMMIT_SHA: ${{ github.sha }}
        BRANCH: ${{ github.ref_name }}
        AUTHOR: ${{ github.actor }}
        RUN_URL: ${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }}
        RESULT_LINT: ${{ needs.lint.result }}
        RESULT_UNIT_TESTS: ${{ needs.unit-tests.result }}
        RESULT_CDK_SYNTH: ${{ needs.cdk-synth.result }}
        RESULT_INTEGRATION: ${{ needs.integration.result }}
        RESULT_EVAL: ${{ needs.eval.result }}
        RESULT_DEPLOY_INFRA: ${{ needs.deploy-infra.result }}
        RESULT_DEPLOY_BACKEND: ${{ needs.deploy-backend.result }}
        RESULT_DEPLOY_FRONTEND: ${{ needs.deploy-frontend.result }}
        DETAIL_LINT: ${{ needs.lint.outputs.detail }}
        DETAIL_UNIT_TESTS: ${{ needs.unit-tests.outputs.detail }}
        DETAIL_EVAL: ${{ needs.eval.outputs.eval_detail }}
        DETAIL_MVP1: ${{ needs.eval.outputs.mvp1_detail }}
      run: python scripts/deploy_report.py
